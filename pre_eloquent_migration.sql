-- This allows for eloquent to see created tables with no preexisting values. Just dummy values. Ignore.
CREATE TABLE legislators (meow0 varchar(1));
CREATE TABLE bills (meow1 varchar(1));
CREATE TABLE users (meow2 varchar(1));
CREATE TABLE favorites (meow3 varchar(1));
